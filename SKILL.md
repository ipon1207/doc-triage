---
name: doc-triage
description: 開発プロジェクトの文書を腐らない形で扱うためのスキル。4 つの用途がある。(1) 技術選定やアーキテクチャの決定を ADR として記録する（「X を採用することにした」「Y で行くことにした」「この決定を残しておきたい」「ADR を書きたい」など、決定を報告されたとき）。(2) 既存の文書・Wiki・README・設計書を渡されて「これで大丈夫か」「更新が追いつかない」「整理したい」と言われたときに、どこが腐るかを診断して直し方を出す。(3) 「これはどこに書けばいい？」という配置の相談に答える。(4) リポジトリやコードベースを調べて文書化の方針を出す（「何から書けばいい」「文書がほとんどない」「このプロジェクトで何を文書化すべきか」と言われたとき）。設計判断・技術選定・ドキュメント整備・README や Wiki や設計書の話題が出たら、明示的に呼ばれていなくてもこのスキルを使うことを検討すること。ただし文書化の一般論や考え方そのものを聞かれているだけなら使わない。これは成果物を出すためのスキルであって、思想を語るためのスキルではない。
---

# 文書トリアージ

開発プロジェクトの文書が腐るのは、書きすぎるからではなく、**更新され続けないと嘘になるものを書いてしまう**から。このスキルは、その判別と、腐らない形への変換を行う。

間違った文書は、文書がないより悪い。読んだ人が信じて動くぶん、被害が大きくなる。だから「とりあえず書いておく」は選択肢に入れない。

## 共通の判断基準

どのモードでも、まずこの 3 つで切る。問い 1 と問い 2 が中心で、問い 3 は文書が混ざっているときにだけ効く。

### 問い 1: それはコードから導出できるか

導出できるもの（フォルダ構成、API の形、クラスやモジュールの一覧、詳細設計の大半）は文書にしない。型定義・スキーマ定義・テストコードに語らせる。文書にすると二重管理になり、必ず片方が嘘になる。

### 問い 2: それは現在形か、過去形か

「今こうなっている」と現在形で主張する文書は腐る。「あの時こう決めた」という過去形の記録は腐らない。

同じ内容でも、過去形に変換できるなら変換する。これがこのスキルの中心的な操作。

### 問い 3: その文書は 1 つの目的だけを担っているか

1 つのファイルの中に「固定したいもの」と「追従させたいもの」が同居していると、どちらの運用もできなくなる。README が典型で、プロジェクトの説明（めったに変わらない）、環境構築手順（頻繁に壊れる）、API 一覧（コードから導出できる）が 1 ファイルに入っていると、全体が更新されないまま放置される。

この症状を見つけたら、中身を直すのではなく**分割する**ことを勧める。分割した時点で、それぞれに別の処方箋（生成に寄せる、実行可能にする、触らない）を適用できるようになる。混ざったままでは、どの処方箋も当てられない。

## 抽象度の 4 レベル

配置を決めるときはこの軸を使う。上ほど変わらず、下ほど変わる。

### レベル 1 — システムの境界（年単位で不変）

このシステムの目的、利用者の種類、外部連携先と何をやり取りするか、責任範囲の外側。

技術名は書かない。図は箱 5〜8 個まで。分量は図 1 枚＋1 ページ。

### レベル 2 — 実行単位の構成（半期単位）

独立して起動・デプロイされる単位の一覧と役割、主要な技術、単位同士の通信方法と向き、永続化先。

ホスト名・URL・バージョン番号は書かない（それは環境一覧の仕事で、腐り方が違う）。分量は図 1 枚＋1 ページ。

### レベル 3 — モジュール構成（月単位）

ここは構造ではなく**ルール**を書く。層の定義と各層の禁止事項、依存の向き、命名規則、横断的な取り決め（トランザクション境界、バリデーションの置き場、エラーの扱い）、迷ったときの判断指針。

モジュール名やファイル名の一覧は書かない。この書き方ならモジュールが 100 個増えても文書は 1 文字も変わらない。それが狙い。分量は 1〜2 ページ。

### レベル 4 — コードレベル（週〜日単位）

文書にしない。使い方はシグネチャと型、振る舞いはテスト、データの形はスキーマが正。

コードコメントに書く価値があるのは why だけで、what と how はコードが語る。

### 全レベル共通

**なぜそう決めたか**は現在形の文書に書かず、日付付きの決定記録として別に積む。

## モードの選び方

会話の状況から、どれをやるか決める。複数にまたがることもある。

| モード | 状況 | 合図 | やること |
| --- | --- | --- | --- |
| A | 決定が報告された | 「X を採用することにした」「Y で行く」「Z はやめて a にした」「この判断を残しておきたい」 | ADR を書く。`references/adr.md` を読んでから進める |
| B | 既存の文書を渡された / 整理したいと言われた | 「この文書どう思う」「Wiki が更新されてない」「設計書を整理したい」「何を残すべきか」 | 診断する。`references/diagnose.md` を読んでから進める |
| C | 配置を聞かれた | 「これどこに書けばいい」「README に書くべき？」「設計書と ADR のどっちに書く？」 | このファイルの範囲で答える。参照ファイルは不要 |
| D | 何から書けばいいか分からない / リポジトリを見て方針を出してほしい | 「このリポジトリ、何を文書化すべき？」「文書がほとんどない」「どこから手をつければいい」 | 調査する。`references/survey.md` を読んでから進める |

既存文書がある状態でモード D を頼まれたら、先にモード B で診断する。腐った文書が残ったまま新しい文書を足すと、どちらを信じるべきか分からなくなる。

## 配置に答えるとき（モード C）

1. まず問い 1 で切る。コードから導出できるなら「書かない、代わりに◯◯に語らせる」と答えて終わり。
2. 導出できないなら、現在形か過去形かで切る。過去形にできるなら ADR へ。
3. 現在形でしか書けないなら、4 レベルのどこかに配置する。
4. 最後に、どの更新トリガーで見直されるかを 1 行添える。トリガーを言えないなら、その文書は腐る前提で分量を絞る。

答えは短くていい。理屈を説明するより、置き場所と理由を 1〜2 行で返すほうが役に立つ。

## やらないこと

- 聞かれていない思想を語る。文書化の考え方そのものを長々と説明するのは、このスキルの仕事ではない。
- テンプレートを先回りして量産する。テンプレートは、実際に何度か書いて毎回同じことを考えていると分かってから作る。先に作ると理想の形を想像で書くことになる。
- 決めなくていいことまで決めた風に書く。特にレベル 3 は、実際に迷いが発生した項目だけ書く。
- 網羅性を目指す。レベル 1〜3 を合計して常時 4〜5 ページに収まるのが健全な状態で、それを超えているなら下のレベルの内容が紛れ込んでいる。
- 推測で背景を埋める。「なぜそうしたか」はコードにもリポジトリにも存在しない。分からないなら人に聞く。もっともらしく補完した記録は、記録がない状態より有害になる。
